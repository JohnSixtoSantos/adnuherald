         
           <div class="content">
                <div class="container-fluid">
                    <div class="col-lg-12 col-md-12">
                            <div class="card">
                                <div class="card-header card-header-warning">
                                    <h4 class="card-title">Centrality Analysis</h4>
                                    <p class="card-category">Select a Tweet collection for centrality analysis</p>
                                </div>
                                <div class="card-body table-responsive">
                                    <table class="table table-hover">
                                        <thead class="text-warning">
                                            <th>Collection Name</th>
                                            <th></th>
                                        </thead>
                                        <tbody>
                                            <% @collections.each do |c| %>
                                                <tr>
                                                    <td><%= c.collection_name %></td>
                                                    
                                                    <td>
                                                        <a href="view_data/<%= c.id %>"><button type="button" class="btn btn-info btn-lg12">View</button></a>
                                                        <!-- Trigger the modal with a button -->
                                                        <button type="button" class="btn btn-info btn-lg12" data-toggle="modal" data-target="#myModal<%= c.id %>">Use Collection</button>

                                                        <!-- Modal -->
                                                        <div id="myModal<%= c.id %>" class="modal fade" role="dialog">
                                                          <div class="modal-dialog">

                                                            <!-- Modal content-->
                                                            <div class="modal-content">
                                                              <div class="modal-header">
                                                                <button type="button" class="close" data-dismiss="modal">&times;</button>
                                                                <h4 class="modal-title">Analysis Confirmation</h4>
                                                              </div>
                                                                <%= form_tag({controller: :centrality, action: :calculate_centrality}) do %>
                                                              <div class="modal-body">
                                                                    Use this collection for centrality analysis?
                                                                    <div>
                                                                    <%= hidden_field_tag(:collection_id, c.id) %>
                                                                    </div>

                                                                    <div>
                                                                        Random Sample Size (leave as empty if all is desired):
                                                                        <%= text_field_tag(:subset_size) %>
                                                                    </div>

                                                              </div>
                                                              <div class="modal-footer">
                                                                    <%= submit_tag("Yes", class: "btn btn-default") %>
                                                                <button type="button" class="btn btn-default" data-dismiss="modal">No</button>
                                                              </div>
                                                                <% end %>
                                                            </div>
                                                            
                                                          </div>
                                                        </div>

                                                    </td>
                                                     <td>
                                                        <a href="centrality/analyses/<%= c.id %>"><button type="button" class="btn btn-info btn-lg12">View Previous Analyses</button></a>
                                                    </td>
                                                </tr>
                                            <% end %>
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </div>

                    
                </div>
            </div> 
        